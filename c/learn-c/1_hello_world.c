// https://www.learn-c.org/en/Hello%2C_World%21

// stdio is a library that provides input and output functions
#include <stdio.h>

// main is the entry point of the program
int main() {
    printf("Hello, World!");
    // we have to return 0 because main is an int function and 
    // it says that the program has run successfully
    return 0;
  }